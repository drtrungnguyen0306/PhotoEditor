<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.fragmgia.photoeditor"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_avatar"
        android:label="@string/title_app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.SplashActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".ui.activity.images.ImagesActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait"/>

        <activity android:name=".ui.activity.function.FunctionActivity"/>

        <activity android:name=".ui.activity.adjust.AdjustActivity"/>

        <activity android:name=".ui.activity.effect.EffectActivity"/>

        <activity android:name=".ui.activity.combine.CombineActivity"/>

        <activity android:name=".ui.activity.multiphoto.MultiImageActivity"/>

        <activity android:name=".ui.activity.audio.AudioActivity"/>

    </application>

</manifest>
